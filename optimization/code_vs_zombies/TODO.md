# TODO

- Execute the complete algorithm in turn 1 until no more time is left (wide
  branch factor). Run the best output in turn 1 and save the state of the tree
  until the computed depth. In turn 2 continue the computation where was left
  in turn 1, and so on.

- Implement a function ACTIONS(s) that return a set of actions (points to move)
  so the solution is more modular
